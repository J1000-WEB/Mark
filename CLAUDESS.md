# MARK 프로젝트 — Claude 작업 가이드

이 문서는 Claude(Cowork/Chat/Code)가 MARK 프로젝트에서 작업할 때 참고하는 컨텍스트입니다.
코드만 봐서는 알기 어려운 "왜 이렇게 만들었는지", "무엇을 다시 시도하면 안 되는지",
"이미 있는 걸 또 만들지 않도록" 하는 정보를 담아둡니다.

> 최종 갱신: 2026-09-01 — 새벽자동화 세션 노트 + 기존 90여개 버전노트(MARK 4.74~6.66) 통합본

---

## 프로젝트 개요

- **MARK**: 패션 브랜드(General Idea, comp_cd=GI001) 매출/재고 분석 대시보드
- Next.js 14 / TypeScript / Tailwind / Google Sheets API, Vercel 배포 (`mark-khaki.vercel.app`)
- 배포: GitHub push → Vercel 자동 배포
- 담당자(소천님)가 매장 운영+데이터 분석 겸임, 이사님이 VMD/재고 쪽 별도 대시보드(`gi-board`) 운영
- 판매데이터 원칙: **가능하면 실제 판매금액/실제 판매가 기준으로 계산, 정가×수량 추정 금지**
  (기본판매가는 표시용일 뿐 계산에 쓰지 않음 — MARK 6.0 데이터사전에서 확립된 원칙)

## ⚠️ 중복 구현 주의 — 같은 일을 두 곳에서 하고 있을 수 있음

새벽 자동화 세션(erp-agent, 로컬 PC에서 Node.js+AutoIt으로 동작)을 만들면서, **대시보드
자체에 이미 있던 기능과 독립적으로 겹치게 재구현한 것들**이 있습니다. 다음에 이 영역을
만질 때는 두 군데 다 확인하고 하나로 합칠지 검토해주세요:

| 기능 | 대시보드 내장(기존) | erp-agent 로컬 자동화(신규) |
|---|---|---|
| 위탁샵(한컬렉션/면세/무신사) 원본→업로드 형식 변환 | `lib/consignmentUpload.ts`, `/consignment-upload` 페이지(수동 드래그앤드롭) | `build-inshop-upload.js`(새벽 무인 자동 다운로드+변환) |
| 한컬렉션 칼라코드 자동수정(풀네임→코드) | `tryAutoFixBarcode()` in consignmentUpload.ts, `COLORCODE` 시트(INSHOP 스프레드시트, 라이브) 참조 | `fixFullColorNameToCode()` in build-inshop-upload.js, 81개 코드 **하드코딩**(라이브 시트 아님) |
| 스타일별채널별(PIP) 대용량 업로드 | `InventoryDashboard.tsx`의 브라우저 업로드(수동) | `upload-style-channel-sheet.js`(새벽 무인) — 둘 다 `/api/upload-style-channel-sheet` 같은 API 호출 |

두 자동수정 로직이 서로 다른 소스(라이브 시트 vs 하드코딩)를 쓰고 있어서, **COLORCODE 시트가
바뀌면 하드코딩된 쪽은 안 따라감** — 나중에 하나로 합치거나 라이브 시트를 읽도록 통일하는 게
좋습니다.

## 인샵매출업로드 원본 파일 형식 (한컬렉션/면세/무신사) — 실측 확정

`lib/consignmentUpload.ts`와 `erp-agent/convert-maechul-to-upload.js` 양쪽에서 독립적으로
확인한 내용이 서로 일치함(교차검증됨):

- **한컬렉션** (`매출일보_YYYYMMDD_HHMMSS.xls`, 구형 xls): 헤더가 고정 행이 아니라
  "판매일자"+"매입거래처" 텍스트로 검색해서 찾아야 함(보통 9~10번째 줄). D열=판매일자,
  K열=매입거래처상품코드(밑줄 뒤 제거해서 바코드로), Q열=실판매가, R열=판매수량.
  **K열이 비어있는 행이 많을 수 있음**(최근 export에서 확인, 38/181건만 채워짐) — 이 경우
  스타일+칼라+사이즈(각각 별도 컬럼)를 조합해서 바코드를 직접 만들어야 함. 사이즈 값에
  앞자리 0 패딩 있음("00S", "0XL") — 제거하고 사용.
- **무신사**: A열 정산일자(ISO)→YYYYMMDD, H열 매장명→점포코드 시트에서 채널번호 조회,
  P열 업체바코드, Q열 판매수량, R열 판매금액. "합계" 행(매장명 없음) 자동 제외.
- **면세(롯데)**: 파일에 날짜 없음 — 업로드 시 날짜 직접 지정 필요. H열=Ref No.(바코드),
  Z열=판매수량(0인 행 제외), AF열 총매출액÷수량=개당단가(0이면 10원 임의설정), 수량이
  ±1 아니면 1개 단위로 분할해서 여러 줄로 만듦.
- ⚠️ **무신사는 2026-08-30부로 완전자동화 중단**. 자동화 접근을 비정상 접근으로 감지해서
  능동적으로 비밀번호+OTP를 초기화시킴(파트너 어드민 보안경고 메일까지 옴). **다시 시도하지
  말 것** — 계정 보안 문제로 번질 위험. 필요하면 대시보드의 수동 업로드 페이지 이용.

## 새벽 자동화 파이프라인 (`erp-agent/run-all.js`, 로컬 PC 전용, 대시보드 코드 아님)

한 CMD 창에서 아래 작업들이 동시에 돌아갑니다 (`run-all.bat`로 실행). **PC 설정 필수**:
설정 → 전원 및 절전 → "화면"을 **"안 함"**으로 (모니터 절전 걸리면 AutoIt 자동화가 새벽에
실패함 — 여러 번 겪은 문제, 지금은 스크립트 자체에도 화면 깨우기 트릭 넣어뒀지만 근본
해결은 절전 자체를 끄는 것).

| 라벨 | 시각 | 파일 | 설명 |
|---|---|---|---|
| 재고/매출 | 상시(00:30~09:30 제외) | `run-loop-interval-quiet.js` | 매장 미운영 시간엔 갱신 안 함 |
| 목표 | 매주 월요일 | `run-monday-schedule.js` | |
| 한컬 | 1시 | `schedule-hankollection.js` | |
| 면세 | 2시 | `schedule-lotte.js` | 00~02시는 정기점검으로 확실히 안 됨(확인됨) |
| 병합 | 1:15, 2:15 | `merge-inshop.js` | 채널 끝난 뒤 여유 두고 파일 갱신 |
| ERP | 2:30 | `schedule-sgerp-upload.js` → `sgerp-login.exe` | AutoIt, ERP 로그인+업로드+저장 |
| ERP끄기 | 2:45 | `schedule-erp-force-close.js` | PA 실행 전 이중 안전장치(강제종료) |
| PA | 2:50 | `schedule-power-automate.js` → `run-power-automate.au3` | 파워오토메이트 앱 직접 열어서 좌표클릭 실행 |
| PIP | 4:00 | `schedule-style-channel-upload.js` | 스타일별채널별 업로드 + 재고이력 저장 |
| 정리 | 4:30 | `schedule-cleanup.js` → `cleanup-4-30.au3` | 크롬/엑셀 강제종료 |

### ERP 업로드 (`sgerp-login.au3`) 핵심 교훈

- ERP(MiPlatform320U.exe)는 **윈도우 프로그램**이라 브라우저 자동화가 아니라 AutoIt으로 진짜
  창 클릭/타이핑을 흉내냅니다.
- **비밀번호 칸은 클릭하면 안 됨** — 앱이 뜨고 준비되면 자동으로 그 칸에 포커스가 가있어서,
  클릭하면 오히려 방해됨. "아이디 칸에 'sjc' 나타날 때까지 대기 → 클릭 없이 바로 타이핑" 방식.
- 사무실 PC에서는 `ControlClick`/`ControlSend`(메시지 기반 자동화)가 막혀있어서, `Send()`(진짜
  키 입력 흉내)만 동작함 — 보안 프로그램이 메시지 기반 입력을 차단하는 것으로 추정.
- `#32770`(표준 대화상자 클래스)은 여러 창이 걸릴 수 있어서, 실제 크기/컨트롤로 진짜 로그인창을
  찾는 `_FindRealLoginWindow()` 함수를 씀.
- 성공/실패 상관없이 **`OnAutoItExitRegister`로 ERP를 항상 종료**시킴 — 안 그러면 뒤에 도는
  파워오토메이트(ERP를 새로 켜는 흐름)가 꼬임.
- 무인 실행 대비: 모든 `MsgBox`에 10초 타임아웃 있음(안 그러면 새벽에 팝업 뜬 채로 영원히 멈춤).

### 파워오토메이트 (`run-power-automate.au3`)

- Power Automate Desktop은 **스토어 버전**이라 `shell:AppsFolder\Microsoft.PowerAutomateDesktop_8wekyb3d8bbwe!PAD.Console`로 실행.
- 처음엔 `.url` 바로가기 방식이었는데 "외부 원본에서 실행" 확인팝업(WPF라 Enter/좌표 다 불안정)
  때문에 **앱을 직접 열어서 목록에서 흐름 선택 → 실행 버튼 클릭**하는 방식으로 전환함.
- 창을 항상 같은 위치/크기로 강제 이동(`WinMove`)한 다음 **상대좌표로 클릭** — 최대화 여부 상관없이
  항상 동작하게. "실행"(▷) 버튼 바로 옆에 빨간 정지 아이콘이 있어서 좌표 잘못 재면 그걸 누르게 됨.

## PIP 파일 (스타일별 채널별 입고/판매/재고현황) 구조 — ⚠️ 중요, 계속 바뀜 주의

- `C:\Users\User\Documents\PIP업데이트YYYYMMDD.xlsx`, 파워오토메이트가 매일 새벽 다운로드.
- 컬럼 구조가 **2026-08월 말에 한 번 바뀜**: "사이즈" 컬럼이 새로 추가되면서(점포 증가 등도 겹침)
  594열 → 604열이 되고, 재고 컬럼 위치가 24 → 25로 밀림. **이런 구조 변경이 또 있을 수 있으니,
  하드코딩된 컬럼 인덱스를 쓰는 곳은 실측 재확인 필요**.
- 현재(2026-09) 기준 확인된 컬럼: `스타일=13, 스타일명=14, 칼라=15, 칼라명=16, 사이즈=17,
  원가=18, 소비자가=19, 판매가=20, 물류(기획21/입고22/출반23/기타24/재고25), 채널(판매26/재고27),
  합계재고28`, 이후 채널별 5개씩(일간/일간금액/주간/누적/재고) 반복.
- `lib/styleChannelCompact.ts`: 마스터 29열(0~28) 그대로 두고, 채널별 반복 블록만 JSON으로
  압축(빈 값 있는 채널만) — 594→31열, 604→31열 둘 다 이 로직 그대로 통함.
- **같은 스타일+컬러가 사이즈별로 여러 행으로 나뉨** — 재고 등 합산이 필요한 곳에서 이 점 놓치면
  "마지막 사이즈 값만 남고 나머지 사라지는" 버그가 남 (실제로 한 번 겪음, 수정함).
- 892만 셀까지 커진 적 있음(구글시트 전체 한도 1000만 셀에 근접) — "지우고 다시쓰기" 방식으로
  전환됨(스테이징 방식은 순간적으로 2배 용량 필요해서 한도 초과). **더 커지면 이 원본 시트 자체를
  구글시트에 만들지 말고 압축 포맷(Daily_Sales_History처럼)으로 바로 저장하는 근본 해결이 필요**
  (MARK 6.58에서 이미 다음 과제로 남겨둔 상태).

## 대시보드 데이터 계층 구조 (기존, 대시보드 코드 자체)

### Daily_Sales_History (MARK_HISTORY 스프레드시트)
- **압축 JSON 포맷**: 날짜+점포당 한 줄, 상세(품번/컬러/사이즈/수량/금액/재고)는 JSON 한 칸에
  압축 저장(MARK 6.6). 예전엔 조합마다 한 줄이라 하루 2만 셀씩 쌓여서 느렸음.
  `expandCompactDailyHistoryRows` 등으로 펼쳐서 씀. 저장할 때마다 구형 데이터가 섞여있어도
  자동으로 압축 형식으로 정리(자가 치유).
- **재고는 스냅샷** — 기간 합산하면 안 되고, 그 기간 중 "가장 최근 날짜" 값만 사용.
- 지난주 수요일(대략 2026-08-중) 전까지는 스타일별채널별(PIP) 시트가 없어서 판매금액이
  **추정치(수량×정가)**로 저장되어 있었음. 이후 백필(`daily-sales-backfill`)로 일부 교체됨 —
  오래된 데이터 다룰 때 이 점 감안.
- RT/재고이관/프로모션 제안은 이제 "금주/전주"(주 1회 갱신) 대신 **Daily_Sales_History 직접
  집계**로 계산함(MARK 6.49, 최대 6일 지연 문제 해결). 단 "주간판매데이터"(weeklyDataProvider)
  쪽은 이 전환에 포함 안 됐었음 — 그리고 그 시스템 자체가 2026-09에 아래처럼 대체됨.

### 판매데이터 탭 — 2026-09 전면 재설계 (예전 시스템 대체)
- **예전(MARK 6.0~6.50대)**: 금주/전주 시트 또는 재고/생산/기간판매×2/재런칭/라인업 6개 파일
  업로드 → `lib/weeklyDataProvider.ts`/`lib/salesDataUpload.ts`가 복잡하게 계산 →
  `Weekly_History`/`Weekly_Snapshot`/`SalesData_Upload_Snapshot`에 저장. **이 구조는 이제 안 씀.**
- **지금(2026-09~)**: 수기 관리하던 "주간판매데이터" 파일을 **통째로 업로드**하면
  `lib/salesDataSuggestions.ts`가 최신 주차 `MM.DD~MM.DD(품번)` 시트를 자동으로 찾아서 파싱,
  제안(추가이관/추가투입/프로모션/단종검토/이월소진+가격제안)까지 생성. `WeeklySalesReport`
  시트(청크 저장)에 결과 저장. **예전 API(`sales-data`, `sales-data-upload`,
  `sales-data-save-report`)와 `Weekly_History` 관련 로직은 이제 안 쓰지만 코드에 남아있을 수
  있음 — 건드릴 때 이 배경 알아두면 헷갈리지 않음.**
- 구글시트 셀 5만자 제한 조심 — 배열을 통째로 JSON화해서 셀 하나에 넣으면 안 되고, **배열 안의
  개별 항목 단위로 청크**를 나눠야 함(이 실수로 실제 장애 남, 수정됨).

### 기존 RT/재고이관 자동 엔진 (재고컨트롤 탭)
- 호조(TOP20)/부진(TOP20 밖) 자동 이관 제안 + **점포요청 RT**(담당자가 품번+요청매장 입력하면
  어디서 보내면 좋을지 계산, MARK 6.9) 둘 다 있음.
- 목표재고 기준: 주간판매 × 3주. 출고 후보는 자기 안전재고 뺀 여유분 많은 순.
- `RT_Result` 시트 매핑: A=보낼채널코드, B=받을채널코드, C=스타일, D=칼라, E=사이즈, F=수량,
  G=승인날짜, H=저장날짜. 같은 스타일이 여러 매장에서 이동해도 "승인날짜+스타일" 기준 1행 통합.
- 오늘(2026-09) 세션에서 만든 **판매데이터탭의 제안 5종류**는 이 기존 RT엔진과 별개 시스템
  (전사 지시가 아니라 주간판매데이터 분석 기반). 둘 다 "제안"이라는 이름이 겹치니 헷갈리지
  않도록 주의.

### 전사지시(Layer 0, VMD탭) — 별개 시스템
- `lib/dataBuilder.ts`의 `styleDirectives`: 매장 확산도(오프라인 12곳↑ × 재고10장↑) 자격필터
  → 창고재고500장 기준 × 2주판매추이로 4분류(주력상품-공급형/소진필요/주력상품-회전형/관찰)
  → 매장별 갭 체크("투입필요" 플래그, 재고10장 미만 매장).
- **이 "투입필요"는 오늘 만든 매장탭의 "재고 대량입고 판매어필" 카드와는 다른 개념** —
  전사지시는 "매장에 부족하니 넣어라", 매장탭 신규카드는 "이미 많이 들어왔는데 안 팔리니
  독려하라". 헷갈리지 않게 구분.

### 온톨로지 레이어 (Channel_Master, Action_Log)
- `Channel_Master` 시트: 채널명→타입(오프라인매장/자사몰/온라인마켓/위탁/면세/기타) 분류.
  `isOnlineChannel()`이 먼저 이걸 확인하고 없으면 키워드 휴리스틱 폴백.
- `Action_Log` 시트: 결정→실행→전후상태 기록. 지금은 Logic Center "반영완료 표시"에만 연결됨 —
  RT이관/가격변경/프로모션 집행에도 연결하면 실제 효과 추적 가능(다음 과제로 남아있음).

### 이메일 알림 인프라 (`lib/alerts.ts`, Resend API)
- 이미 구축되어 있고 **재사용 대상**: 일간매출 스냅샷 누락 알림, PIP 대용량 업로드 실패 알림
  등에서 이미 씀. 새 알림 기능 만들 때 이 인프라 그대로 재사용하면 됨.
- 환경변수: `RESEND_API_KEY`, `ALERT_EMAIL_TO`, `ALERT_EMAIL_FROM`(선택).
- Vercel 크론 개수 이미 여러 개 등록되어 있음(일간/주간/월간 스냅샷, 날씨, 스페셜오퍼실적,
  주간목표캡처, 일간매출체크 등) — 요금제 크론 개수 제한 있을 수 있으니 새 크론 추가 시 확인.

### 실시간재고조회 + 바코드 스캔 (매장탭 하단, StoreBriefV2.tsx)
- `/api/store-stock-lookup`: 품번 입력 → 컬러별(+최근 데이터는 사이즈별) 현재재고.
  재고는 최근 날짜 스냅샷, 합산 아님.
- 이 매장 이력엔 없지만 상품360(전사기준)엔 있는 컬러는 **"이 매장 정보 없음"으로 정직하게
  표시**(숫자 지어내지 않음).
- **2026-08-03 이전 데이터는 사이즈 자리에 판매가가 잘못 들어가있던 버그**가 있었음
  (`looksLikeRealSize()`로 필터링해서 회피) — 오늘 세션에서 발견한 PIP 사이즈컬럼 이슈와는
  별개 건이지만, 비슷한 시기에 데이터 포맷 문제가 반복됐던 걸 보면 원본 데이터 소스 자체가
  종종 구조 변경되는 것으로 보임.
- 오늘(2026-09) 세션에서 사진업로드/카메라스캔 인식률 개선함(qrbox 제한 제거, 9구역 타일링
  재시도) — 이건 기존 `formatsToSupport`(CODE_128 등) 설정과 같이 동작.

## 재고 대량입고 알림 (재고컨트롤 탭, 오늘 신규)

- `재고입고이력` 시트(최근 10일 롤링 보관) + `재고입고알림_활성` 시트(상태 유지) 두 개로 관리.
- **"한번 뜨면 확인 전에 사라지면 안 되고, 재고가 실제로 줄면(투입/판매) 사라져야 한다"**는 요구사항
  때문에, 단순 "2일전 대비 비교"가 아니라 **활성 알림 상태를 저장**하는 방식으로 만듦.
- 기준: 100장 이상 증가, 2일 전 대비, 해제는 peak 대비 30% 감소.
- PIP 파일의 스타일+컬러별 재고(사이즈 합산 필요)를 매일 업로드 시 같이 저장.

## 매장탭(StoreBriefV2.tsx) 히스토리

- 최초 버전(MARK 6.56): 날씨행동지침, 우리매장 특화상품(전사대비 1.8배↑), 재고워닝, 최근입고
  신상품(21일 이내+재고>0). storeOutperformers 최소 3개 이상 판매된 것만 후보(노이즈 방지).
- 오늘(2026-09) 추가/변경: 품번표시, 매출 반올림표시(`man()`, 소수점 없이), 날씨 최저/최고기온
  표시, 판매급증 리뷰(최근3일 vs 이전14일 비교, storeOutperformers와는 다른 개념 — 시간축 비교),
  재고 대량입고 판매어필 카드(신규), 재고확인/재고회전율 카드는 **삭제**(중복/혼란스럽다는 피드백).
- 이번달 진척 카드 아래 평일/주말 필요매출 카드 추가(주말=평일의 1.3배 가중치, 실측 데이터
  생기면 조정 필요).

## 상품 이미지 (`components/ProductThumb.tsx`)

- gi-board 이미지 API를 서버 프록시로 호출(`GI_BOARD_TOKEN` 환경변수, 브라우저에 토큰 노출 안 함).
- 세션 내 캐싱, 썸네일 우선 시도 후 원본 폴백, 없으면 "No Img" 플레이스홀더.
- 매장별 TOP10/재고컨트롤 RT카드/주간매출 TOP상품 등 이미 여러 곳에 적용되어 있음 — 새 카드
  만들 때 그냥 `<ProductThumb styleCode={...} size={...} />` 재사용하면 됨.

## 과거 데이터 정리 도구 (일회성 스크립트, 스케줄에는 안 들어감, erp-agent 폴더)

- `fix-old-barcodes.js`: 기존 인샵매출 형식 파일의 바코드만 일괄 자동수정(같은 파일에 덮어씀, `.bak` 백업).
- `convert-maechul-to-upload.js`: ERP 원본 "매출일보" 파일(헤더가 10번째줄, 스타일/칼라/사이즈
  분리형)을 인샵매출업로드 형식으로 변환. 바코드 없으면 스타일+칼라+사이즈로 직접 조합. 반품(마이너스
  수량) 포함. 여러 파일을 날짜 상관없이 파일 하나로 합쳐서 저장.

## 알려진 스프레드시트 ID (참고용, 바뀔 수 있음)

| 용도 | ID |
|---|---|
| 스페셜오퍼위크 세부일정 | `1KfiwexgTnPIrBaV4G7B2c_aXtvhvUoCnjyXAxc_cZN4` |
| 인샵매출 UPLOAD 시트 | `1531ifBVtAkMWSl2IGLcydLfYRe2_L8V-xHLvGQXLd3M` |
| 주간전용(MARK_WEEKLY_HISTORY, 예전 시스템, 지금은 비활성) | `19cSF8l67-qHl6s3MhEXwGzDaIFVQVg2WS6EWHLyB57A` |

## 일반 원칙

- **"이사님이라도 못 믿는다"**: gi-board VMD 데이터도 MARK에서 교차검증
  (`lib/vmdCrossCheck.ts`, 차이가 절대2개↑ 그리고 30%↑일 때만 "불일치"로 판정, 사소한 시차는 무시).
- 요일별 매출 비교 규칙: 화/수/목/일요일엔 "어제"와 비교, 금/토/월요일엔 "전주 같은 요일"과 비교
  (`getComparisonDateForDaily`, MARK 6.40).
- 모든 자동화 스크립트는 실패해도 프로세스가 안 죽고 다음날 재시도되도록 설계(재시도 로직,
  MsgBox 타임아웃, 에러 시 조용히 넘어가되 로그는 남김).
- 새 기능 만들 때는 항상 **실제 파일로 미리 검증**(파싱 결과, 청크 크기 등)하고 배포.
- 큰 시트 업로드는 "지우고 다시쓰기" 패턴(스테이징 방식은 순간적으로 2배 용량 필요해서 위험).
