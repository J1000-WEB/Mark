# MARK 6.5 RT TOP20 + Grouped View Patch

## 목적
- RT 이동 제안 대상을 전사 판매순위 TOP 20 품번으로 넓히고, 결과 화면을 품번 단위로 묶어서 보기 쉽게 개선.

## 변경 전 문제
- RT 대상 품번 자체는 이미 전사 TOP 20 안에서만 뽑히고 있었지만, 최종 화면에는 `rtSuggestions.slice(0, 10)`로 "제안 건수" 자체를 10건으로 강제 컷하고 있었음.
- 하나의 품번이 여러 매장으로 이동하면 여러 건(카드)을 만들기 때문에, 실제로는 2~3개 품번의 제안이 10건을 다 차지하고 나머지 TOP 20 품번은 화면에 아예 안 보일 수 있었음.
- 화면도 단순 카드 나열이라 품번이 늘어나면 스크롤이 길어지고 어떤 품번이 몇 건 제안됐는지 한눈에 안 보였음.

## 변경 내용

### lib/dataBuilder.ts
- RT 대상 품번 기준을 매직넘버 없이 `RT_ELIGIBLE_RANK = 20`으로 명시 (기존 `companyRank <= 50` → `companyRank <= RT_ELIGIBLE_RANK`).
- 최종 결과의 `.slice(0, 10)` 하드컷 제거. TOP 20 품번에서 나온 제안은 모두 노출하되, 안전장치로 `RT_SUGGESTION_SAFETY_CAP = 300`만 유지.
- 응답에 `rtEligibleProductRank`(=20), `rtSuggestionProductCount`(제안이 실제로 발생한 품번 수) 추가.

### components/InventoryDashboard.tsx
- `RTSuggestionSection`을 품번(styleCode) 단위로 그룹핑하는 구조로 변경 (`groupRtItems`).
- 그룹 헤더에 전사순위, 상품명, 품번, 제안 건수, 총 이동수량, 상태별(승인/보류/거절) 카운트 배지 표시.
- 그룹별 접기/펼치기 지원, 상단에 "전체 펼치기 / 전체 접기" 버튼 추가.
- 상단 요약 문구: "전사 판매 TOP 20 품번 중 N개 품번에서 제안 발생 · 총 M건".
- 기존 상태 필터 탭(전체/제안만/승인/보류/거절), 승인날짜 선택, RT 지시서 다운로드, 개별 카드(RTCard)의 승인/보류/거절 동작은 그대로 유지.

## 확인 포인트
- RT 이동 제안 상단 요약에 "TOP 20 품번 중 N개 품번"이 표시되는지 확인.
- 품번 그룹 헤더를 클릭하면 해당 품번의 이동 제안 카드들이 펼쳐지는지 확인.
- 전체 펼치기/접기 버튼이 모든 그룹에 동작하는지 확인.
- 승인/보류/거절 후 그룹 헤더의 상태 배지 카운트가 올라가는지 확인.
- RT 지시서 다운로드가 기존과 동일하게 동작하는지 확인 (로직 변경 없음).
