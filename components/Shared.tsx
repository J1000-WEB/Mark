import { pct, won, num } from "@/lib/mark";
export function Kpi({t,v,s}:{t:string;v:string;s?:string}){return <div className="rounded-3xl bg-white p-5 shadow-sm"><p className="text-sm font-semibold text-slate-500">{t}</p><p className="mt-3 text-2xl font-black">{v}</p>{s&&<p className="mt-1 text-sm font-bold text-slate-500">{s}</p>}</div>}
export function Card({t,children,right}:{t:string;children:React.ReactNode;right?:React.ReactNode}){return <section className="rounded-3xl bg-white p-5 shadow-sm"><div className="mb-4 flex items-center justify-between gap-3"><h2 className="text-xl font-black">{t}</h2>{right}</div>{children}</section>}
export function Empty(){return <div className="rounded-2xl bg-slate-50 p-6 text-center text-sm text-slate-500">표시할 데이터가 없습니다.</div>}
export function Info({l,v,c}:{l:string;v:string;c?:string}){return <div className="rounded-xl bg-slate-50 p-3"><p className="text-xs text-slate-500">{l}</p><p className={`mt-1 font-bold ${c==="red"?"text-red-600":c==="blue"?"text-blue-600":""}`}>{v}</p></div>}
export function ProductList({items}:{items:any[]}){if(!items?.length)return <Empty/>;return <div className="space-y-3">{items.map((p,i)=><div key={`${p.styleCode}-${i}`} className="rounded-2xl border border-slate-100 bg-white p-4"><div className="flex justify-between gap-4"><div><p className="text-sm text-slate-500">#{i+1} · {p.styleCode}</p><p className="font-black">{p.productName}</p></div><div className="text-right"><p className="text-lg font-black">{won(p.weekAmount)}</p><p className="text-xs text-slate-500">금주 매출</p></div></div><div className="mt-3 grid grid-cols-2 gap-2 text-sm md:grid-cols-4"><Info l="판매수량" v={`${num(p.weekNet)}개`}/><Info l="전주매출" v={won(p.prevAmount)}/><Info l="매출 신장률" v={`${p.amountChangeRate>=0?"+":""}${pct(p.amountChangeRate)}`} c={p.amountChangeRate<0?"red":"blue"}/><Info l="기여도" v={pct(p.contributionRate)}/></div></div>)}</div>}
